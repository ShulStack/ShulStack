{
  "name": "@shulstack/cms",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "generate": "echo 'cms generators are driven from the web app via Payload CLI'",
    "lint": "biome check .",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@payloadcms/richtext-lexical": "^3.80.0",
    "payload": "^3.80.0"
  }
}
